import { ipcRenderer } from "electron";
import {ADD_VIDEO} from '../reducers/videos_reducer.js';

export const addVideos = videos => dispatch => {
    console.log("sample addVideo log statement");
};
